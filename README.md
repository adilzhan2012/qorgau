# Qorgau — акустический мониторинг леса

Прототип системы охраны особо охраняемых природных территорий. Датчики на
базе ESP32-S3 слушают лес и сами решают, что слышат и опасно ли это:

| Класс | Что это | Для парка |
|---|---|---|
| **Природа** | листва, ветер, вода, дождь | безопасно |
| **Птицы, звери** | пение, крики | безопасно |
| **Тишина** | фон, ничего заметного | безопасно |
| **Человек** | голос, речь, смех | **опасно** |
| **Собака** | лай — рядом человек | **опасно** |
| **Транспорт** | двигатель машины, мотоцикла | **опасно** |
| **Бензопила** | незаконная рубка | **опасно** |
| **Выстрел** | браконьеры | **опасно** |

— с процентами уверенности и объяснением, почему принято такое решение.
Опасный звук — это тревога на карте и красный светодиод на плате.

Живая карта датчиков, обновление в реальном времени, распознавание работает
без интернета и без облачных ИИ-сервисов. Устройства добавляются прямо на
карте; плата, подключённая по USB, сама находит своё устройство по ID.

### ▶ Открыть сайт: https://adilzhan2012.github.io/qorgau/

Ничего скачивать и устанавливать не нужно — откройте ссылку в **Chrome или Edge**
и нажимайте кнопки со звуками. Чтобы подключить плату, нажмите
«Подключить плату ESP32» — один раз, дальше она подключается сама.

Собрать плату, прошить, подключить GPS и выложить всё на GitHub — пошагово
в **[SETUP.md](SETUP.md)**.

> Учебный проект. Первое развёртывание задумано в Иле-Алатауском национальном
> парке под Алматы.

---

## Главная идея

**Никаких нейросетей и облаков: восемь классов — это восемь наборов признаков
с весами, и каждый вердикт можно объяснить.**

ESP32-S3 снимает звук с микрофона (INMP441 или ICS-43434), считает быстрое
преобразование Фурье и вытаскивает из каждого кадра в 64 мс 26 чисел:

| Признак | Что означает |
|---|---|
| `rms` | громкость, дБFS |
| `zcr` | частота переходов через ноль — высокая у шипения, низкая у гула |
| `attack` | насколько резко нарос звук |
| `harmonic` | есть ли у звука высота тона — у двигателя есть, у выстрела нет |
| `flatness` | «шумность» спектра: 1 — белый шум, 0 — чистый тон |
| `spread` | размазана ли энергия по спектру |
| `crest` | пик к среднему: у хлопка большой, у ровного гула маленький |
| `centroid` | высоко или низко звучит: гул ~0.1, свист птицы ~0.9 |
| `tonal` | насколько самая громкая частота выше средней |
| `flux` | насколько форма спектра сдвинулась с прошлого кадра |
| `bands[16]` | энергия в 16 логарифмических полосах от 60 Гц до 8 кГц |

### Решение принимается не по кадру, а по полутора секундам

Это главное, чем система отличается от первой версии. За 64 мс лай собаки,
упавшая с ветки капля, треск сучка и хлопок выстрела выглядят одинаково:
коротко, громко, широкополосно. Человек в такой ситуации делает очевидное —
слушает подольше.

Плата копит последние **24 кадра** и считает по ним то, чего у отдельного
кадра нет в принципе:

| Признак окна | Что означает | Кто так звучит |
|---|---|---|
| `duty` | какую долю времени звук держится у своего максимума | мотор и дождь — всё время, лай — треть, выстрел — два кадра из двадцати четырёх |
| `onsets` | сколько раз в секунду громкость резко подскочила | лай и хлопки — часто, бензопила — ни разу |
| `levelSpan` | насколько пик выше фона окна | выстрел и лай — на тридцать децибел, ветер — на три |
| `steady` | насколько неподвижен спектр | двигатель ~1, поющая птица ~0 |
| `attack` | самый резкий подъём громкости внутри окна | выстрел ~1 |

Бензопила — это звук, который **не прекращается**; лай — несколько отдельных
выкриков **с паузами**; выстрел — одиночный хлопок **на фоне тишины**. Разница
не в спектре мгновения, а в поведении во времени, и классификатор смотрит
именно на него (`src/lib/audio/window.ts`).

### Обязательные условия

Признаки класса складываются во взвешенное среднее, но часть из них —
обязательные (в коде `gate`): они не складываются, а умножают. «Бензопила» без
непрерывного звука невозможна, сколько бы ни совпало остального; «выстрел» без
резкого удара и тишины вокруг — тоже. Провалившееся обязательное условие
оставляет классу десятую часть балла, так что вердикт остаётся сравнением,
а не запретом, и его по-прежнему можно объяснить словами.

По этим числам плата **сама** решает, что это за звук, и шлёт вердикт вместе
с признаками. Тот же классификатор, строка в строку, живёт и на сайте
(`src/lib/audio/classify.ts`): он разбирает звук с микрофона ноутбука и файлы,
а вердикт платы показывает рядом — расхождение было бы видно, а не спрятано.
Что копия остаётся копией, проверяет `npm run fw-check`: он вырезает кусок C
из прошивки, собирает его и сравнивает вердикты с браузерными на трёхстах
случайных наборах признаков.

Вердикт — это несколько байт. Именно поэтому схема рассчитана на LoRaWAN:
передавать сам звук не нужно, и датчик может годами работать от батареи.

---

## Насколько хорошо оно слышит

Честный ответ на этот вопрос важнее самого распознавания, поэтому качество
здесь **измеряется**, а не утверждается.

`npm run corpus` скачивает 200 полевых записей из набора [ESC-50](https://github.com/karoldvl/ESC-50)
(настоящие собаки, бензопилы, двигатели, дождь, ветер, гроза, птицы, стрёкот,
капель, костёр, человеческий голос), `npm run eval` прогоняет их через тот же
конвейер, что работает в браузере, и печатает, сколько было **пропусков
опасного** и сколько **ложных тревог**. Каждая запись проверяется пять раз: как есть, тише на 18 дБ,
издалека (глухо и с фоном), под шум леса и под ветер — иначе легко подогнать
пороги под чистые демо-файлы и решить, что всё работает.

| | было (решение по кадру) | стало (окно 1.5 с) |
|---|---|---|
| Верных вердиктов | 36 % (63 из 177) | **71 % (149 из 209)** |
| Ложных тревог | 95 | **25** |
| Пропусков опасного | 19 | **35** |

<sub>200 полевых записей плюс девять синтезированных, без помех. С пятью
условиями (тише, далеко, шум леса, ветер) — 72 % на 1045 прогонах: качество не
держится на чистоте записи.</sub>

### Класс «человек» — самый слабый, и вот насколько

Голос добавлен последним, и он тянет общие цифры вниз: из тридцати записей
человеческих звуков ловятся **семь**, а из двадцати пяти ложных тревог
**двенадцать** — это его работа. Без него было 79 % и 15 пропусков.

Причина не в порогах, а в подходе. Речь узнаётся не по спектру мгновения, а по
тому, как спектр движется от слога к слогу, и десяток чисел на окно такое
движение описывают грубо. Кудахтанье курицы, кваканье и плеск воды попадают в
те же диапазоны. Это ровно тот класс, где обученная модель на мел-спектрах
(тот же Edge Impulse) сделает лучше, чем пороги — и где для обучения нужны
записи речи с этого самого микрофона.

Оставлен он потому, что для парка голос — самый прямой признак: лай, мотор и
пила говорят о человеке косвенно, голос напрямую. Но выдавать его за рабочий
было бы неправдой, поэтому здесь написано, сколько он ловит.

Важная оговорка про этот корпус: все записи в нём сделаны вблизи источника и
потому громкие. Пороги, подобранные только по нему, начинают считать громкость
приметой класса — у «бензопилы» такой признак был и его пришлось убрать, когда
пила из колонок в метре от ноутбука прочиталась как «природа». Громкость
говорит, как далеко источник, а не что это за источник; в парке датчик обязан
слышать пилу за двести метров.

Что именно остаётся неправильным — видно из того же отчёта, и это честнее
круглых цифр. Ложные тревоги: кудахтанье кур принято за лай и за человека,
гроза и капель — за выстрел, ветер и жужжание насекомых — за пилу и мотор, шум
воды — за человека. Пропуски: далёкий фейерверк, которым мы заменяем выстрел
(записей выстрелов в открытых наборах нет), тихий двигатель, одиночный лай и
бензопила, которую включают и глушат — в окне остаются паузы, а
«непрерывность» для пилы обязательна.

Для парка это разумный размен: ложная тревога приходит ночью и никого не
находит, а пила работает минутами — следующее окно её поймает. Но написать
«100 %» было бы неправдой, поэтому здесь написано, сколько есть.

Проверить всё это можно за две минуты:

```bash
npm run selftest   # восемь синтезированных звуков, ответ известен заранее
npm run corpus     # скачать полевые записи (~80 МБ, один раз)
npm run eval -- --dir corpus          # сколько ложных тревог и пропусков
npm run eval -- --dir corpus --why    # почему принят именно такой вердикт
npm run fw-check   # прошивка и сайт отвечают одинаково
```

---

## Камера: второй ответ на тот же вопрос

Звук говорит «похоже на человека» и иногда ошибается — кудахтанье и плеск воды
попадают в те же признаки. Камера отвечает на тот же вопрос совсем другим
способом, и когда оба согласны, это уже не догадка: две независимые ложные
тревоги одновременно — событие настолько редкое, что им можно пренебречь.

В панели датчиков есть кнопка **«Включить камеру»**. Модель — COCO-SSD
(lite_mobilenet_v2) на TensorFlow.js, из восьмидесяти её классов нас интересует
один: `person`. Человека в кадре обводит рамка с процентом, а подтверждённое
появление попадает в журнал тревог как «Человек · камера ноутбука».

Три оговорки, без которых это было бы обманом:

- **Камера работает с ноутбуком, не с платой.** Обычную USB-камеру (например
  X-Game XW-79) к ESP32-S3 подключить нельзя: у него нет ни USB-хоста с
  драйвером UVC, ни памяти под видео. Для камеры в лесу нужна другая железка —
  ESP32-CAM или ESP32-S3-EYE с модулем OV2640. Здесь камера — это пост
  оператора.
- **Камере нужен интернет при первом включении:** модель (около восьми
  мегабайт) подтягивается с CDN. Звук работает офлайн, камера — нет. Зато в
  сборке сайта по-прежнему ноль новых зависимостей.
- **Кадры не сохраняются и никуда не отправляются.** Видео живёт в памяти
  браузера, модель смотрит на него и забывает. Это не лень: фотография
  узнаваемого человека — персональные данные (в Казахстане закон 152-V ЗРК), и
  складывать их «чтобы было» — значит взять на себя обязательства, которых
  учебный проект не выполнит. В журнал пишется факт и время, а не лицо.

---

## Как это устроено

```
ESP32-S3 + микрофон I2S + GPS
   I2S 16 кГц, кадры по 1024 отсчёта (64 мс)
   → БПФ 1024 → 16 полос + 10 признаков кадра
   → окно в 24 кадра (1.5 с): ровность, паузы, перепад, резкие начала
   → классификатор → вердикт (класс, %, опасно?) + светодиод
   → строка JSON в USB Serial (115200): признаки, вердикт, GPS
                    ↓
   браузер (Web Serial) ─┐
   микрофон ноутбука ────┼→ тот же классификатор в браузере → карта и карточки
   проигрывание файла ───┘
```

Один набор признаков, один классификатор в двух копиях (C на плате, TypeScript
на сайте), три источника звука.

Сервера нет вообще: сайт — это статические файлы, а классификация считается
в браузере. Поэтому его можно открыть по ссылке на любом ноутбуке.

**Новых зависимостей — ноль.** БПФ, чтение COM-порта, разбор WAV и офлайн-
проверка написаны вручную, чтобы перед демонстрацией ничего не пришлось
устанавливать.

---

## Запуск у себя (не обязательно)

Сайт уже опубликован по ссылке выше, и для демонстрации этого достаточно.
Локальный запуск нужен, только если хотите менять код:

```bash
npm install
npm run dev
```

Открыть **http://localhost:3100** в **Chrome или Edge** — Web Serial есть
только в них.

**Ключей, аккаунтов и базы данных не нужно.** Список устройств хранится в
браузере, распознавание считается там же.

---

## Устройства

Кнопка **+** над списком: название, ID и точка на карте — кликом по карте,
геолокацией ноутбука или координатами. В карточке устройства — изменить,
переставить, удалить, журнал тревог.

Статус честный: **В сети** — только у того, что присылает звук прямо сейчас,
**Тревога** — когда классификатор уверен, остальные — **Нет сигнала**.
Последние показания и тревоги помнятся между перезагрузками.

Список, показания и журнал лежат в localStorage этого браузера.

### Проверить, что классификатор действительно работает

Страница **[/selftest](https://adilzhan2012.github.io/qorgau/selftest/)** синтезирует восемь звуков с заранее
известными ответами — от тишины и водопада до выстрела — и показывает, что
классификатор решил и почему. Сейчас — 8 из 8. Сигналы генерируются заново
при каждом обновлении страницы.

---

## Три способа показать распознавание

| Способ | Как | Когда использовать |
|---|---|---|
| **Кнопки со звуками** | Панель «Датчики» → Выстрел / Собака / Бензопила | Самый надёжный, не зависит от шума и железа |
| **Микрофон ноутбука** | Кнопка «Микрофон ноутбука», разрешить доступ | Самый живой: хлопок в ладоши → «Выстрел» |
| **ESP32 по USB** | «Подключить плату ESP32», выбрать порт | Настоящее железо |

Плата представляется по ID (`{"id":"QRG-003",...}`) и попадает в устройство с
таким же ID; незнакомый ID становится новым устройством на карте. ID можно
записать в плату с сайта — он хранится во флеше, перепрошивать не нужно.
Порт выбирается один раз: дальше Chrome открывает его сам при каждом
подключении. Плат может быть несколько.

Подробная пошаговая инструкция с подключением платы и разбором ошибок —
в **[DEMO.md](DEMO.md)**.

### Свои звуки

Положить `.wav` или `.mp3` в `public/audio/` и запустить `npm run audio` —
кнопки появятся сами, код менять не надо. Класс определяется по имени файла:
`gunshot`, `dog`/`лай`, `chainsaw`/`пила`, `vehicle`/`машина`, `ambient`/`лес`.

Файлы в комплекте синтезированы тем же скриптом; свои записи он не
перезаписывает.

---

## Железо

| Компонент | Роль |
|---|---|
| **ESP32-S3** (N8R2) | слушает, считает признаки, читает GPS |
| **INMP441** или **ICS-43434** | цифровой I2S-микрофон — любой из двух, распиновка одинаковая |
| **GPS-модуль** с UART (NEO-6M, NEO-M8N, ATGM336H…) | координаты датчика; необязателен |

Микрофон: `VDD→3V3`, `GND→GND`, `L/R→GND`, `SCK/BCLK→GPIO14`, `WS/LRCL→GPIO15`,
`SD/DOUT→GPIO16`. GPS: `VCC→3V3`, `GND→GND`, `TX→GPIO18`, `RX→GPIO17`
(последний можно не подключать). Пины меняются `#define`-ами в начале скетча.
**Не используйте GPIO 33–37** — на S3 они заняты PSRAM.

Плата шлёт координаты раз в две секунды, и устройство **само встаёт на карту**,
как только GPS поймал спутники. Встроенный RGB-светодиод показывает вердикт
без ноутбука: зелёный — безопасно, красный — опасно, жёлтый мигает — GPS ищет
спутники. Схема с картинкой и разбор состояний — в
[DEMO.md](DEMO.md#схема-подключения).

Прошивка при старте сама проверяет микрофон и пишет в порт, что именно не так
с подключением, если что-то не так — результат виден и на сайте.

Заряд батареи плата шлёт, если в скетче задан `PIN_BATTERY` (делитель 1:1
с Li-ion на ADC-пин). На USB заряд не измеряется, и сайт показывает «—», а не
выдуманное число.

> **Arduino UNO для этой задачи не подходит.** У ATmega328P нет блока I2S,
> 2 КБ оперативной памяти и 16 МГц — БПФ на 1024 точки туда не поместится,
> а INMP441 — цифровой микрофон по I2S. Слушает только ESP32-S3.

Скетч: [`firmware/qorgau_sensor/qorgau_sensor.ino`](firmware/qorgau_sensor/qorgau_sensor.ino).
Собирается и на ядре ESP32 2.x, и на 3.x — версия I2S-драйвера выбирается
автоматически.

---

## Команды

| Команда | Что делает |
|---|---|
| `npm run dev` | сайт на http://localhost:3100 |
| `npm run build` | продакшн-сборка |
| `npm run typecheck` | проверка типов |
| `npm run audio` | сгенерировать демо-звуки в `public/audio/` |
| `npm run deploy` | пересобрать и опубликовать сайт |

---

## Структура

| Путь | Зачем |
|---|---|
| `src/lib/audio/features.ts` | измерение признаков кадра |
| `src/lib/audio/classify.ts` | **классификатор** — здесь крутятся пороги |
| `src/lib/audio/fft.ts` | БПФ, без зависимостей |
| `src/lib/audio/sources.ts` | микрофон и проигрывание файлов |
| `src/lib/audio/serialSensor.ts` | чтение платы через Web Serial |
| `src/lib/live/readings.ts` | вердикт по признакам + защёлка тревоги |
| `src/lib/live/history.ts` | что помнится между сессиями |
| `src/lib/devices/store.ts` | список устройств в localStorage |
| `src/hooks/useReadings.ts` | показания и журнал тревог |
| `src/hooks/useDevices.ts` | статус устройства: в сети / тревога / нет сигнала |
| `src/hooks/useBoards.ts` | платы по USB: автоподключение, привязка по ID, GPS |
| `src/components/DeviceForm.tsx` | добавить / изменить / переставить устройство |
| `src/app/selftest/` | проверка классификатора |
| `firmware/qorgau_sensor/` | прошивка ESP32-S3 |
| `scripts/deploy.mjs` | публикация сайта на GitHub Pages |

---

## Где данные

Всё — в браузере того ноутбука, где открыт сайт: список устройств
(`qorgau.devices.v1` в localStorage), последние показания, журнал тревог и
привязки плат. Сервера и облака нет, ключей и аккаунтов не требуется —
склонировал, `npm install`, `npm run dev`.

Для настоящего парка с сотней датчиков этот слой меняется на любую базу:
хук `useDeviceStore` — единственное место, которое знает, где лежит список.

---

## Карты

MapLibre GL, не Mapbox — ни токена, ни аккаунта. Три бесплатных стиля в
`src/lib/mapStyles.ts`: тёмный (Esri Dark Gray Canvas), спутник (Esri World
Imagery) и рельеф (OpenTopoMap).

Подпись об источнике тайлов внизу слева убирать нельзя — это условие
лицензии. Для продакшена стоит взять MapTiler, Stadia или Mapbox: Esri не
публикует явной открытой лицензии на эти адреса.
